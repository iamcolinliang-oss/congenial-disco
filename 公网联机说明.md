# 公网联机说明

现在可以不在同一个 Wi-Fi 也一起玩。

## 最简单玩法

把这三个文件一起上传到支持 Node.js 的云服务器：

- `kof97_98_style_fighter.html`
- `remote-server.js`
- `package.json`

启动命令：

```bash
npm start
```

云服务器启动后会得到一个网址，例如：

```text
https://你的服务器.onrender.com
```

两个人都打开这个网址，进入游戏里的“远程联机”：

1. 房主点“创建房间”
2. 另一个人输入同一个房间号，点“加入房间”
3. 房主点“开始联机战斗”

如果页面是 `https://` 打开的，联机地址会自动变成：

```text
wss://你的服务器.onrender.com
```

## 本机测试

在电脑上运行：

```bash
node remote-server.js
```

然后打开：

```text
http://localhost:8080
```

本机测试时联机地址是：

```text
ws://localhost:8080
```
